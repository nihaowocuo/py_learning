# 1.字符串格式化
# 例如输出，我叫什么，我住在哪，我今年多少岁，我喜欢做什么
# name = input("请输入你的姓名：")
# address = input("请输入你的住址：")
# age = int(input("请输入你的年龄："))
# hobby = input("请输入你的爱好：")
#
# # %s字符串占位的含义，也可以放入其他类型的占位，相当于万能的
# # %d整数占位，%f占位小数
# str = "我叫%s，住在%s，年龄%d，爱好是%s。" % (name,address,age,hobby)
# # 当后续只有一个变量时，也可以无需括号
# str_0 = "我叫%s。" % name
# print(str)
# # 通用占位
# str_1 = "我叫{}，住在{}，年龄{}，爱好是{}。".format(name,address,age,hobby)
# print(str_1)
# # f-string新型格式化
# str_2 = f"我叫{name}，住在{address}，年龄{age}，爱好是{hobby}。"
# print(str_2)

# 2.索引和切片
# 索引：按照位置提取元素。例如我直接从我爱发明里，提取出其中某一个字符
# 取第几个第几个元素
# str_3 = "我是垂杨"
# print(str)
# # 从零开始数
# print(str_3[0])
# print(str_3[1])
# print(str_3[2])
# print(str_3[3])
# # 也可以采用负数进行倒数检索-1即为最后一个字
# print(str_3[-1])
# print(str_3[-2])
# 切片：提取其中的一部分内容，而非单一的内容
# 从哪取到哪
# str_4 = "我来自深渊，而你来自前缘"
# 注意：后续的5是指的是整体的索引5，而非从索引1开始，往后取5位
# 同时索引5是取不到
# print(str_4[1:5])
#如果你要从开头直接取数据，则可以略写
# print(str_4[:5])
# 同时从中间到结尾也可以略写结尾
# print(str_4[7:])
# 能否一次性取多个切片？
# 注意，当前仍不可以倒数切
# print(str_4[-1:-3])
# 此为全取
# print(str_4[:])
# print(str_4)

# 但我们可以给切片添加步长来控制切片的方向，以此达到倒切
# str_5 = "天下第一"
# 语法:s[start:end:step]
# 此为倒取
# print(str_5[::-1])
# 从开头开始取,取到结尾,每隔两个取一次,此次取了0和2,0为天,2为第
# 取两个包含此前的字符,比如0123,[::2]step为2,(打组为[01],[23]
# 则每隔两个取一次,
# 实际上为打组,把两个字符打成一组,由于step是正数,所以取组合中左边第一个
# 从0开始取,依次输出0,2.实际只隔开了一个字符
# print(str_5[:3:2])
# str_6 = "abcdefghijklmnop"
# print(str_6)
# step正数从左取,负数从右取
# print(str_6[2:14:2])
# 我尝试,使用负数,以此达成从左到右输出,但每次打组取最右侧的
# 但并未成功,我发现我的操作好像是不合理的
# print(str_6[2:14:-2])
# 以下为测试
# 此为全输出,且是从左到右输出
# print(str_6[:])
# print(str_6[::2])
# 此为倒输出,此处我未定义所以,py中应该默认的是从-1的结尾到开头
# print(str_6[::-1])
# 那我如果强制定义为从左到右的正数输出呢
# 为何此次可以输出第一位a
# print(str_6[0::-1])
# 但我若是定义了范围,则一次输出都没
# print(str_6[0:12:-1])
# 我开始测试是否只有[start:end:step]均为同一符号才可以使用
# 此无法输出
# print(str_6[-1:-12:2])
# 但进行符号修改后成功了,并且是从右往左打组,每两个为一组,且取最右侧的(由于是负数)
# print(str_6[-1:-12:-2])
#那我该如何实现,倒输出的同时,打组取最左侧的数呢?
# 我能否这样实现呢
# step > 0：要从 start **向右（索引增大）**走，所以必须 start实际 <= stop实际
# step < 0：要从 start **向左（索引减小）**走，所以必须 start实际 >= stop实际
# 以下便违背了规则
# print(str_6[-12:-1:-2])
# 实际上的取负数,是会被转成正数的,因此,没法使用这种格式print(str_6[-1:-12:2])
# print(str_6[-1:-12:-2])
# 也就是说,这是与正负数无关的,其实际上是与顺序有关
# print(str_6[15:4:-2])
# 这两行代码运行效果一样,只是采用负数更为方便?
# 也就是说默认方向是从左往右，但是step为负数的话会进行，强制更改方向顺序，并且检测start与stop的数值是否符合step顺序，若不符合，则返回空串？
# -2的含义是从右向左出发,而12在右侧,所以2向左出发是根本无法达到12的
# print(str_6[2:12:-2])
# 改为2后则正常
# print(str_6[2:12:2])
# 此处打印出c,而非返回空串则意味着此程序是正确的,我们先把-12转换成正数,-12 + 16 = 4
# 也就说,其实际上为[2:4:2],是符合顺序的因此会打印出第一个字符c,而之后的就已经达到stop的范围限制了
# print(str_6[2:-12:2])
# 这样呢,-9 + 16 = 7,实际为[7:12:2]向右取
# print(str_6[-9:12:2])
# print(str_6[7:12:2])
# 这两个实际运行结果一样,
# 方向只由 step 决定；负 start/stop 只是换算成固定正索引，
# 可能恰在 start 右侧（同向），也可能在左侧（反向）。
# 必须先换算，再看 start 与 stop 的实际位置关系。
# [-1:-12:2] 空是因为换算后 15 > 4 反向；[2:-12:2] 不空是因为换算后 2 <= 4 同向。

# 3.字符串的常规操作
# 字符串的操作是不会对原字符串进行修改,而是产生一个新的字符串
# str_7 = "python"
# capitalize()此方法用于将字符串首字母大写,其他字母小写
# str_7.capitalize()
# 此时打印的仍为原字符串
# print(str_7)
# s = str_7.capitalize()
# print(s)
# 字符串首字母大写,其他字母小写
# s = "pytHon"
# s1 = s.capitalize()
# print(s1)
# 仅仅字符串中第一个大写,
# s = "python is good"
# s1 = s.capitalize()
# print(s1)
# 如果是中文呢?中文保持不变

# 把每个英文单词的首位进行大写，其对单词的判断就是不间断，连续的，
# 如果在单词中途插入其他内容，其将不被认为是一个单词
# s = "I have a dream"
# s1 = s.title()
# print(s1)

# 即这种情况
# s = "I ha哈哈ve a dream"
# s1 = s.title()
# print(s1)

# 字符串内全部内容小写
# s = "I HAve a dream"
# s1 = s.lower()
# print(s1)

# 同理大写
# 此应用常用来统一大小写验证码判断
# s = "I have a dream"
# s1 = s.upper()
# print(s1)

# 那为何不采用lower？
# lower在处理一些欧洲的英文时会出现问题，无法处理
# verify_code = "Xado"
# user_input = input(f"请输入验证码：（{verify_code}）")
# 但这是否会有安全的风险，既然已经有了统一的升格为大写，那我发送的验证码为何还要又大小写
# if user_input.upper() == verify_code.upper():
#     print("验证码正确！")
# else:
#     print("验证码不正确！")

# 字符串的替换和切割
# s = "    你好，   我是垂杨    ！  "
# 去除左右两端的空格
# s1 = s.strip()
# print(s)
# print(s1)

# 实际例子
# 如果在名字或者代码后加入空格，此时采用肉眼是无法发现的，这样便会导致登录失败
# user_name = input("请输入你的用户名：")
# user_password = input("请输入你的密码：")

# 而这种便可以去除两侧的空格
# user_name = input("请输入你的用户名：").strip()
# user_password = input("请输入你的密码：").strip()
#
# if user_name == "admin":
#     if user_password == "123456":
#         print("登陆成功！")
#     else:
#         print("登陆失败！")
# else:
#     print("登陆失败！")

# strip()的作用不只是去除字符串两侧的空白符（空格，\t制表符，就是缩进Tab。\n为换行符)

# 要去除所有的空格，包括字符中间的空格
# 要采用替换
# s = "你好啊 我是  垂杨"
# 此处我在垂杨后加个空格，就会出现问题，无法替换
# s1 = s.replace("垂杨 ", "天下")
# s1 = s.replace("垂杨", "天下")
# print(s)
# print(s1)

# s = "I have a dream!"
# 如果前后都有呢，是否有类似strip的功能
# s = "   I have a dream!  "
# 测试成功
# 去除所有的空格
# s1 = s.replace(" ", "")
# print(s1)

# split:切割，该用什么切割？
# 字符串切割
# a = "python_java_c++_javascript"
# 通过_切割,切割后的内容放入列表中
# lst = a.split("_")
# 用java去切
# lst = a.split("java")
# 输出结果中java损失了，用什么去切割，则什么会被损失
# print(lst)

# 今日重点，strip(),replace(),solit()
