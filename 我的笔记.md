# 我的笔记

> 本文件用于记录个人学习笔记、想法、待办与随记。可随时在文末追加新内容。

## 笔记

- Python 没有真正的常量机制；约定上用**全大写字母**命名的变量表示常量（如 `MAX_SIZE = 100`），仅作约定提醒，**语法层面仍可被修改**。

- 字符串可用 `+` 拼接成完整句子（如 `'Hello' + ' ' + 'World'`）；`*` 可重复（如 `'ab'*3`）；但不能直接与数字相加，需 `str()` 转换或用 f-string / `join()`。
- 布尔类型可直接用表达式赋值，如 `a = 5 > 3` 得到 `True`；本质是赋值右侧表达式的 bool 结果（如 `is_adult = age >= 18`）。

- Python 语法特点：① 变量无需声明类型（动态类型，赋值即定义，类型运行时确定）；② `if`/`for`/`while` 条件**不用括号**（如 `if x > 0:`，可加 `()` 但非必须）；③ 用冒号 `:` 引导、靠**缩进**划分代码块（通常 4 空格，Tab/空格混用会报错）；④ 类型注解 `x: int = 5` 是**可选**的。

- `if`/`elif`/`else` 流程：`if` 条件为真执行；`elif`（= else if）可多个、依次判断；`else` 收尾（所有条件都不满足时执行，可选）。`elif`/`else` 后都要跟冒号并缩进。

- 循环（while）要点：① 条件里的变量需**提前定义/初始化**，否则 `NameError`；② Python 中 `0`、空串、`None`、空列表等都是**假值（falsy）**，`while x:` 且 `x=0` 时循环直接跳过不执行；③ 从 0 计数应写 `while x < n:` 而非 `while x:`。

- while 循环条件写法：① 条件是**表达式**（如 `while x < n:`）时，变量初始化为 0 没问题，循环正常进入；② 条件**只是变量本身**（如 `while x:`）时，不能简单初始化为 0 或 1——`x=0` 是假值导致**无法进入循环**，`x=1`(非零真值)若循环体不改它则**死循环**；③ 用变量当条件要确保循环体能改变它或有 `break`。
