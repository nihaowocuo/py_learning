# 项目长期记忆 (MEMORY.md)

## Python 学习问答工作流（用户约定）

本目录 `E:\py_learning` 用于 Python 学习。用户遇到 Python 问题时会提问，处理流程固定如下：

1. **解决问题并解释原因**：给出可运行的代码/答案，并解释「为什么」(原理)。
2. **回答风格（2026-07-12 更新）**：
   - **聊天回复**：详细讲解，原理、对比、示例都展开说。
   - **写入文件**：只写关键部分，尽量精简，不堆砌冗余说明。
3. **生成 3 个文档**（均放在工作区根目录 `E:\py_learning`）：
   - `python_trouble`：原样记录用户所问的问题。
   - `python_answer`：只写解答的**关键要点**（精简，不展开冗长解释）。
   - `Python_sum`：汇总**精简后的问题** + **精简后的关键答复**。
4. 每轮问答后**直接追加到对应文档末尾**即可，**无需预先 Read / 通读历史内容**（2026-07-12 用户优化：避免每次问答都先读大文件）。追加用 Bash 追加重定向（`cat >> file <<'EOF'`）即可；除非用户要求重建，否则不覆盖历史。

约定来源：用户 2026-07-12 明确指示，当前阶段「无需处理」，待提问时再执行；同日补充"聊天详细、文件精简"风格要求。
