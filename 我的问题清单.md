# 我的问题清单

> 在 `E:\py_learning` 学习 Python 期间提出的所有问题。原始提问见 `python_trouble.md`，精简解答见 `python_answer.md`，汇总见 `Python_sum.md`。本文件为统一索引。

## 问题列表（共 9 个，均已解答）

| # | 问题 | 状态 |
|---|------|------|
| 1 | 如何检测电脑已安装 Python 与解释器，并查找安装位置？ | 已解答 |
| 2 | 为何 D 盘与 C 盘均有 Python，如何查看解释器是否已安装？ | 已解答 |
| 3 | Python 解释器与环境的关系？无解释器能否写代码但无法运行测试？ | 已解答 |
| 4 | 是否需要将 `D:\py\Scripts` 加入环境变量 PATH？ | 已解答 |
| 5 | 为何环境变量里看不到 `py`？ | 已解答 |
| 6 | 退出 Python 只用 `exit()` 吗？`quit` 也能退出（是数据库命令？） | 已解答 |
| 7 | Python 计算中无需转换类型、会自动转换吗？ | 已解答 |
| 8 | 为什么 `"当前剩余的钱：" + money` 报错 `TypeError`？ | 已解答 |
| 9 | `==` 能判字符串相等，其他类型相等怎么判断（`==` 与 `is` 区别）？ | 已解答 |

## 说明

- 第 1–5 题（2026-07-12）聚焦 **Python 安装与环境配置**：检测/定位解释器、D 盘与 C 盘 python.exe 区别、解释器与环境关系、Scripts 加入 PATH、`py` 不在环境变量原因。
- 第 6–9 题聚焦 **Python 语法与基础机制**：退出指令、类型自动转换、字符串与数字拼接报错、相等判断 `==`/`is`。
- 另有个人学习笔记（常量约定、字符串连接、布尔赋值、语法特点、if/elif/else、while 循环等）记录在 `我的笔记.md`，不在此问题清单内。
